/* import React from "react";

export default function Item({ title, id, image }) {
    return (
        <>
            <h4>{title}</h4>
            <small>{id}</small>
            <img alt={title} src={image} />

        </>
    )
}
 */